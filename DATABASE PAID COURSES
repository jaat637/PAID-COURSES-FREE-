<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>PAID COURSES FREE - Secure Access</title>
  <style>
    /* Basic Reset */
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background: linear-gradient(135deg, #000000, #8B0000, #FF0000);
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
    }

    .container {
      background: rgba(17, 17, 17, 0.95);
      border: 2px solid #FF0000;
      box-shadow: 0 0 25px #FF0000;
      padding: 35px 25px;
      border-radius: 20px;
      width: 100%;
      max-width: 400px;
      text-align: center;
      backdrop-filter: blur(5px);
    }

    h2 {
      color: #FF4C4C;
      font-size: 26px;
      margin-bottom: 25px;
      font-weight: bold;
      text-shadow: 0 0 8px red;
    }

    .tabs {
      display: flex;
      margin-bottom: 20px;
      background: rgba(255, 255, 255, 0.05);
      border-radius: 0.8rem;
      overflow: hidden;
    }

    .tab {
      flex: 1;
      padding: 0.8rem;
      text-align: center;
      cursor: pointer;
      color: rgba(255, 255, 255, 0.7);
      font-weight: 600;
      transition: all 0.3s ease;
      border-bottom: 2px solid transparent;
    }

    .tab.active {
      background: rgba(255, 255, 255, 0.1);
      color: #ffffff;
      border-bottom: 2px solid #FF0000;
    }

    .tab:hover {
      background: rgba(255, 255, 255, 0.08);
      color: #ffffff;
    }

    .tab-content {
      display: none;
    }

    .tab-content.active {
      display: block;
      animation: fadeSlide 0.4s ease;
    }

    @keyframes fadeSlide {
      from { opacity: 0; transform: translateY(10px); }
      to { opacity: 1; transform: translateY(0); }
    }

    input {
      width: 100%;
      padding: 14px;
      border-radius: 10px;
      border: none;
      margin-bottom: 20px;
      font-size: 17px;
      background: #222;
      color: #fff;
      box-shadow: inset 0 0 6px red;
    }

    input::placeholder {
      color: rgba(255, 255, 255, 0.6);
    }

    button {
      width: 100%;
      padding: 14px;
      border: none;
      border-radius: 10px;
      margin: 10px 0;
      font-size: 17px;
      font-weight: bold;
      cursor: pointer;
      color: white;
      background: linear-gradient(90deg, #ff0000, #990000);
      box-shadow: 0 0 18px red;
      transition: transform 0.3s;
    }

    button:hover {
      transform: scale(1.06);
      background: linear-gradient(90deg, #ff4d4d, #cc0000);
    }

    .whatsapp-btn {
      display: block;
      width: 100%;
      padding: 13px;
      border-radius: 10px;
      text-align: center;
      text-decoration: none;
      margin: 12px 0;
      font-size: 17px;
      font-weight: bold;
      background: linear-gradient(90deg, #25D366, #128C7E);
      box-shadow: 0 0 18px #25D366;
      color: white;
      transition: transform 0.3s;
    }

    .whatsapp-btn:hover {
      transform: scale(1.06);
      background: linear-gradient(90deg, #128C7E, #25D366);
    }

    #responseBox {
      color: #fff;
      margin-top: 22px;
      font-size: 16px;
      display: none;
      animation: fadeIn 1.2s ease-in-out;
      background: #1c1c1c;
      border: 1px solid #444;
      padding: 18px;
      border-radius: 12px;
      text-align: left;
      white-space: pre-line;
      box-shadow: 0 0 12px #ff0000;
    }

    @keyframes fadeIn {
      0% { opacity: 0; transform: translateY(10px); }
      100% { opacity: 1; transform: translateY(0); }
    }

    .hidden {
      display: none;
    }
  </style>
</head>
<body>
  <div class="container">
    <h2>🚀 PAID COURSES FREE 🔥</h2>
    <div class="tabs">
      <div class="tab active" onclick="showTab('login')">Login</div>
      <div class="tab" onclick="showTab('signup')">Sign Up</div>
    </div>

    <!-- Login Form -->
    <div id="login" class="tab-content active">
      <form id="loginForm">
        <input type="text" id="loginUsername" placeholder="Username" required>
        <input type="password" id="loginPassword" placeholder="Password" required>
        <button type="submit">Login</button>
      </form>
    </div>

    <!-- Sign-Up Form -->
    <div id="signup" class="tab-content">
      <form id="signupForm">
        <input type="text" id="signupUsername" placeholder="Username" required oninput="validateUsername(this)">
        <input type="password" id="signupPassword" placeholder="Password (min 6 characters)" required oninput="validatePassword(this)">
        <button type="submit">Sign Up</button>
      </form>
    </div>

    <!-- SIM Details Section -->
    <div id="simDetailsSection" class="hidden">
      <input type="text" id="number" placeholder="📱 Enter phone number e.g. 03XXXXXXXXX" required />
      <button onclick="fetchData()">🔍 Get SIM Details</button>

      <div id="responseBox"></div>

      <button id="shareButton" class="whatsapp-btn" onclick="shareWhatsApp()" style="display: none;">
        📤 Share Result on WhatsApp
      </button>

      <a
        class="whatsapp-btn"
        href="https://whatsapp.com/channel/0029VbA6VdqA2pL4pBhOnI3R"
        target="_blank"
      >
        🔥 Join Our WhatsApp Channel for Free Courses
      </a>
    </div>
  </div>

  <script>
    // Tab switching functionality
    function showTab(tab) {
      document.querySelectorAll('.tab').forEach(btn => btn.classList.remove('active'));
      document.querySelectorAll('.tab-content').forEach(content => content.classList.remove('active'));
      document.querySelector(`#${tab}`).classList.add('active');
      document.querySelector(`.tab[onclick="showTab('${tab}')"]`).classList.add('active');
    }

    // Username validation
    function validateUsername(input) {
      input.value = input.value.replace(/[^a-zA-Z0-9_]/g, '');
      if (input.value.length < 3) {
        input.style.borderColor = '#dc2626';
      } else {
        input.style.borderColor = '#1e3a8a';
      }
    }

    // Password validation
    function validatePassword(input) {
      if (input.value.length < 6) {
        input.style.borderColor = '#dc2626';
      } else {
        input.style.borderColor = '#1e3a8a';
      }
    }

    // Handle login form submission
    document.getElementById('loginForm').addEventListener('submit', function(e) {
      e.preventDefault();
      // Placeholder for authentication logic
      // In a real application, you'd verify credentials here
      // For demonstration, we'll assume login is successful
      document.getElementById('login').classList.remove('active');
      document.querySelector('.tabs').classList.add('hidden');
      document.getElementById('simDetailsSection').classList.remove('hidden');
    });

    // Handle signup form submission
    document.getElementById('signupForm').addEventListener('submit', function(e) {
      e.preventDefault();
      // Placeholder for signup logic
      // In a real application, you'd save user credentials here
      // For demonstration, we'll assume signup is successful and switch to login tab
      showTab('login');
      alert('Signup successful! Please log in.');
    });

    // Fetch SIM details
    async function fetchData() {
      const number = document.getElementById("number").value.trim();
      const responseBox = document.getElementById("responseBox");
      const shareButton = document.getElementById("shareButton");

      if (!number) {
        alert("❗ Please enter a phone number.");
        return;
      }

      const url = `https://api.allorigins.win/get?url=${encodeURIComponent(
        `https://amscriptapi.vercel.app/AhmadMods/Num=${number}`
      )}`;

      try {
        const res = await fetch(url);
        const json = await res.json();
        const apiData = JSON.parse(json.contents);

        if (apiData.status === "success12
